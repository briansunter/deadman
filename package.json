{
  "name": "deadman",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "cf-typegen": "wrangler types",
    "typecheck": "bunx tsc --noEmit --pretty false",
    "check": "bun run typecheck && bun run test",
    "test": "for test_file in src/*.test.ts; do bun test \"$test_file\" || exit 1; done"
  },
  "devDependencies": {
    "bun-types": "^1.3.10",
    "typescript": "^5",
    "wrangler": "^4.71.0"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
